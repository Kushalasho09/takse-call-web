name: takse_call_web
description: "Takse Call Web Admin & Analytics Dashboard"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  intl: ^0.20.2
  firebase_core: ^3.12.1
  firebase_auth: ^5.5.1
  firebase_auth_platform_interface: ^7.7.3
  cloud_firestore: ^5.6.5
  shared_preferences: ^2.5.5
  web: ^1.1.0
  http: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
