import 'package:flutter/material.dart';

class AppColors {
  // Primary Brand Colors (from Takse Call App)
  static const Color primary = Color(0xFF2563EB); // Vibrant Royal Blue
  static const Color primaryDark = Color(0xFF1D4ED8);
  static const Color primaryLight = Color(0xFF3B82F6);
  static const Color primarySubtle = Color(0xFFEFF6FF);

  static const Color accent = Color(0xFF0D9488); // Teal
  static const Color accentLight = Color(0xFF14B8A6);

  // Status & Metric Colors
  static const Color incoming = Color(0xFF10B981); // Vibrant Emerald Green
  static const Color incomingSubtle = Color(0xFFECFDF5);

  static const Color outgoing = Color(0xFFF59E0B); // Amber Orange
  static const Color outgoingSubtle = Color(0xFFFFFBEB);

  static const Color missed = Color(0xFFEF4444); // Crimson Red
  static const Color missedSubtle = Color(0xFFFEF2F2);

  static const Color rejected = Color(0xFFE11D48); // Rose Red
  static const Color rejectedSubtle = Color(0xFFFFF1F2);

  static const Color neverAttended = Color(0xFFFB7185); // Light Coral
  static const Color notPickupByClient = Color(0xFFFB7185); // Light Coral

  static const Color connected = Color(0xFF2563EB); // Primary Blue
  static const Color workingHours = Color(0xFF475569); // Slate Grey

  // Background & Surface
  static const Color scaffoldBackground = Color(0xFFF4F6F9); // Light Gray Surface
  static const Color cardBackground = Color(0xFFFFFFFF);
  static const Color sidebarBackground = Color(0xFFFFFFFF);
  static const Color topBarBackground = Color(0xFFFFFFFF);

  // Borders & Dividers
  static const Color border = Color(0xFFE2E8F0);
  static const Color borderSubtle = Color(0xFFF1F5F9);

  // Text Colors
  static const Color textPrimary = Color(0xFF0F172A); // Dark Slate
  static const Color textSecondary = Color(0xFF64748B); // Muted Slate
  static const Color textMuted = Color(0xFF94A3B8); // Light Muted
  static const Color textLight = Color(0xFFFFFFFF);

  // Badges & Accents
  static const Color connectCodeText = Color(0xFFD97706); // Warm Amber
  static const Color connectCodeBg = Color(0xFFFEF3C7);
  static const Color floatingChat = Color(0xFF0284C7); // Cyan Blue
}
