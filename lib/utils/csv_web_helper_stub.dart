void pickCsvFileWeb(void Function(String content, String fileName, int fileSize) onFileLoaded) {
  // No-op on VM / native platforms
}

void downloadCsvWeb(String content, String filename) {
  // No-op on VM / native platforms
}
